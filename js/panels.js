// Using vanilla js

module.export = ( function (){

    // store nodes
    var tabmoduleContainer = document.getElementsByClassName('module__container--centered')[0]; // first item in array of classes
    console.log(tabmoduleContainer);

} )();
